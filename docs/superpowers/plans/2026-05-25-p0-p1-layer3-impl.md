# P0-P1 Layer 3 Implementation Plan

> Continuation of main plan. Depends on Layer 0-2 being complete.
> Covers: Frontend pages for all modules and integration tests.

---

### Task 12: Implement Asset Frontend (Hooks + Pages)

**Files:**
- Create: `src/features/assets/hooks.ts`
- Create: `src/features/assets/AssetForm.tsx`
- Create: `src/features/assets/AssetTable.tsx`
- Modify: `src/app/(dashboard)/assets/page.tsx`
- Modify: `src/app/(dashboard)/assets/new/page.tsx`
- Modify: `src/app/(dashboard)/assets/[id]/page.tsx`

- [ ] **Step 1: Create asset React Query hooks**

Create `src/features/assets/hooks.ts` with useAssets, useAsset, useCreateAsset, useUpdateAsset, useDeleteAsset hooks using apiFetch and React Query.

- [ ] **Step 2: Create AssetTable component**

Create `src/features/assets/AssetTable.tsx` — data table with type/market filters, columns: name, type, market, quantity, price, marketValue, costAmount, profit. Delete button with confirm dialog.

- [ ] **Step 3: Create AssetForm component**

Create `src/features/assets/AssetForm.tsx` — shared create/edit form. Dynamic fields based on assetType (cash hides symbol/market). Uses useCreateAsset or useUpdateAsset based on assetId prop.

- [ ] **Step 4: Update asset pages to use components**

Replace placeholder pages with client components using AssetTable and AssetForm.

- [ ] **Step 5: Verify typecheck**

Run: `pnpm typecheck`

- [ ] **Step 6: Commit**

```bash
git add src/features/assets src/app/\(dashboard\)/assets
git commit -m "feat: implement asset frontend (table, form, pages)"
```

---

### Task 13: Implement Dashboard Frontend

**Files:**
- Create: `src/features/dashboard/hooks.ts`
- Create: `src/features/dashboard/TotalAssetCard.tsx`
- Create: `src/features/dashboard/PieChart.tsx`
- Create: `src/features/dashboard/RecentTransactions.tsx`
- Create: `src/features/dashboard/GoalProgressCard.tsx`
- Modify: `src/app/(dashboard)/dashboard/page.tsx`

- [ ] **Step 1: Create dashboard hook and components**

- `hooks.ts`: useDashboard() → GET /api/dashboard
- `TotalAssetCard.tsx`: displays totalAssetValue, totalCost, totalProfit, totalProfitRate
- `PieChart.tsx`: reusable ECharts donut chart (echarts/core tree-shaking)
- `RecentTransactions.tsx`: simple list of last 5 transactions
- `GoalProgressCard.tsx`: progress bar + remaining + monthly suggestion

- [ ] **Step 2: Update dashboard page**

Compose all components. Show empty state with CTA when no assets.

- [ ] **Step 3: Verify typecheck**

Run: `pnpm typecheck`

- [ ] **Step 4: Commit**

```bash
git add src/features/dashboard src/app/\(dashboard\)/dashboard
git commit -m "feat: implement dashboard frontend with charts"
```

---

### Task 14: Implement Transaction Frontend

**Files:**
- Create: `src/features/transactions/hooks.ts`
- Create: `src/features/transactions/TransactionTable.tsx`
- Create: `src/features/transactions/TransactionForm.tsx`
- Modify: `src/app/(dashboard)/transactions/page.tsx`
- Modify: `src/app/(dashboard)/transactions/new/page.tsx`

- [ ] **Step 1: Create transaction hooks**

useTransactions(filters, pagination), useCreateTransaction, useDeleteTransaction.

- [ ] **Step 2: Create TransactionTable**

Table with filters (asset select, type select, date range) and pagination controls.

- [ ] **Step 3: Create TransactionForm**

Select asset → fill type, quantity, price, fee, time, reason, emotionTag.

- [ ] **Step 4: Update pages**

- [ ] **Step 5: Verify and commit**

```bash
git add src/features/transactions src/app/\(dashboard\)/transactions
git commit -m "feat: implement transaction frontend"
```

---

### Task 15: Implement Goals Frontend

**Files:**
- Create: `src/features/goals/hooks.ts`
- Create: `src/features/goals/GoalCard.tsx`
- Create: `src/features/goals/GoalForm.tsx`
- Modify: `src/app/(dashboard)/goals/page.tsx`

- [ ] **Step 1: Create goal hooks**

useActiveGoal(), useCreateGoal(), useUpdateGoal(), useDeleteGoal().

- [ ] **Step 2: Create GoalCard and GoalForm**

GoalCard shows active goal progress. GoalForm for create/edit with name, amount, date, initialPrincipal.

- [ ] **Step 3: Update goals page**

Show GoalCard if active goal exists, otherwise show GoalForm.

- [ ] **Step 4: Verify and commit**

```bash
git add src/features/goals src/app/\(dashboard\)/goals
git commit -m "feat: implement goals frontend"
```

---

### Task 16: Implement Exposure Frontend

**Files:**
- Create: `src/features/exposure/hooks.ts`
- Create: `src/features/exposure/ExposureTable.tsx`
- Modify: `src/app/(dashboard)/exposure/page.tsx`

- [ ] **Step 1: Create exposure hooks**

useFundsExposure() → GET /api/exposure/funds.

- [ ] **Step 2: Create ExposureTable**

Table: holdingName, holdingSymbol, holdingMarket, industry, exposureAmount, sourceFundSymbols. Reuse PieChart for industry/market allocation.

- [ ] **Step 3: Update exposure page**

Show table + charts. Empty state when no fund assets.

- [ ] **Step 4: Verify and commit**

```bash
git add src/features/exposure src/app/\(dashboard\)/exposure
git commit -m "feat: implement exposure frontend"
```

---

### Task 17: Implement Watchlist Frontend

**Files:**
- Create: `src/features/watchlist/hooks.ts`
- Create: `src/features/watchlist/WatchlistTable.tsx`
- Create: `src/features/watchlist/AddWatchlistDialog.tsx`
- Modify: `src/app/(dashboard)/watchlist/page.tsx`

- [ ] **Step 1: Create watchlist hooks**

useWatchlist(), useAddWatchlist(), useRemoveWatchlist(), useSearchAssets(keyword).

- [ ] **Step 2: Create WatchlistTable**

Table: name, symbol, type, market, price. Delete button per row.

- [ ] **Step 3: Create AddWatchlistDialog**

Dialog with search input → calls /api/market-data/search → shows results → click to add.

- [ ] **Step 4: Update watchlist page**

- [ ] **Step 5: Verify and commit**

```bash
git add src/features/watchlist src/app/\(dashboard\)/watchlist
git commit -m "feat: implement watchlist frontend"
```

---

### Task 18: Integration Tests

**Files:**
- Create: `tests/unit/services/auth.test.ts`
- Create: `tests/unit/services/assets.test.ts`
- Create: `tests/unit/services/transactions.test.ts`
- Create: `tests/unit/services/goals.test.ts`
- Create: `tests/unit/services/exposure.test.ts`

- [ ] **Step 1: Auth service tests**

Test: register success, duplicate email 409, login success, wrong password 401.

- [ ] **Step 2: Asset service tests**

Test: create auto-calculates costAmount/marketValue, list with filters, soft delete, ownership check returns null.

- [ ] **Step 3: Transaction service tests**

Test: create buy increases quantity and recalculates avgCost, create sell decreases quantity keeps avgCost.

- [ ] **Step 4: Goal service tests**

Test: create rejects when active exists (409), getActiveGoal calculates progress.

- [ ] **Step 5: Exposure service tests**

Test: aggregates duplicate holdings, calculates industry/market allocation.

- [ ] **Step 6: Run all tests**

Run: `pnpm test`

Expected: all pass.

- [ ] **Step 7: Commit**

```bash
git add tests/unit/services
git commit -m "test: add service layer integration tests"
```

---

### Task 19: Final Verification

- [ ] **Step 1: Full verification**

Run:

```bash
pnpm typecheck
pnpm test
pnpm build
```

Expected: all exit with code 0.

- [ ] **Step 2: Manual smoke test**

Start `pnpm dev`, verify in browser:
- `/login` and `/register` render forms
- After login → redirects to `/dashboard`
- `/assets` shows table, `/assets/new` shows form
- All other pages render without errors

- [ ] **Step 3: Fix and commit any issues**
